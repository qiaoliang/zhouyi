# 周易通APP - 前端开发环境搭建指南

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-01-11 |
| 适用平台 | iOS / Android / 微信小程序 / H5 |
| 技术栈 | React Native + Taro + React.js |

---

## 目录

1. [通用开发环境](#1-通用开发环境)
2. [iOS开发环境](#2-ios开发环境)
3. [Android开发环境](#3-android开发环境)
4. [微信小程序开发环境](#4-微信小程序开发环境)
5. [H5开发环境](#5-h5开发环境)
6. [项目初始化](#6-项目初始化)
7. [代码规范配置](#7-代码规范配置)
8. [环境验证](#8-环境验证)
9. [常见问题](#9-常见问题)

---

## 1. 通用开发环境

### 1.1 必需软件

#### Node.js 和 npm

```bash
# 推荐版本
Node.js: 18.x LTS 或 20.x LTS
npm: 9.x 或更高

# 检查已安装版本
node --version
npm --version

# 如果未安装，从官网下载安装
# https://nodejs.org/
```

#### 包管理器选择

```bash
# npm (默认)
npm install

# yarn (可选，更快的安装速度)
npm install -g yarn
yarn install

# pnpm (推荐，节省磁盘空间)
npm install -g pnpm
pnpm install
```

**本项目推荐**: pnpm

---

#### Git

```bash
# macOS
brew install git

# Windows
# 下载 Git for Windows
# https://git-scm.com/download/win

# Linux (Ubuntu/Debian)
sudo apt-get install git

# 验证安装
git --version
```

---

#### 代码编辑器

**推荐**: Visual Studio Code

```bash
# 下载地址
# https://code.visualstudio.com/

# 推荐安装的 VS Code 插件
- ES7+ React/Redux/React-Native snippets
- Prettier - Code formatter
- ESLint
- TypeScript Vue Plugin (Volar)
- Auto Rename Tag
- Auto Close Tag
- Path Intellisense
- GitLens
- Material Icon Theme
- One Dark Pro
```

---

### 1.2 React Native CLI

```bash
# 安装 React Native CLI
npm install -g react-native-cli

# 或使用 npx (推荐，无需全局安装)
npx react-native --version
```

---

## 2. iOS开发环境

### 2.1 系统要求

- **操作系统**: macOS 13 (Ventura) 或更高
- **Xcode**: 15.x 或更高
- **CocoaPods**: 1.14 或更高

---

### 2.2 安装 Xcode

```bash
# 从 Mac App Store 安装 Xcode
# 搜索 "Xcode" 并安装

# 安装 Xcode Command Line Tools
xcode-select --install

# 验证安装
xcodebuild -version

# 同意 Xcode 许可协议
sudo xcodebuild -license accept
```

---

### 2.3 安装 CocoaPods

CocoaPods 是 iOS 项目的依赖管理工具。

```bash
# 安装 CocoaPods
sudo gem install cocoapods

# 验证安装
pod --version

# 设置 CocoaPods (首次使用)
pod setup
```

---

### 2.4 安装 Watchman (可选)

Watchman 是 Facebook 开发的文件监听工具，可以提高开发效率。

```bash
# macOS
brew install watchman

# 验证安装
watchman --version
```

---

### 2.5 iOS 模拟器

```bash
# 列出可用的模拟器
xcrun simctl list devices

# 启动特定模拟器
xcrun simctl boot "iPhone 15 Pro"

# 打开模拟器应用
open -a Simulator
```

---

### 2.6 环境配置

```bash
# 添加到 ~/.zshrc 或 ~/.bash_profile
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8

# 重新加载配置
source ~/.zshrc
```

---

## 3. Android开发环境

### 3.1 系统要求

- **操作系统**: Windows 10/11, macOS, 或 Linux
- **JDK**: JDK 17
- **Android Studio**: 2023.1 (Hedgehog) 或更高
- **Android SDK**: API Level 33 (Android 13) 或更高

---

### 3.2 安装 JDK

```bash
# macOS
brew install openjdk@17

# 设置环境变量
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
source ~/.zshrc

# 验证安装
java -version
javac -version
```

**Windows**:
1. 下载 JDK 17: https://adoptium.net/
2. 安装后配置 JAVA_HOME 环境变量
3. 将 %JAVA_HOME%\bin 添加到 PATH

---

### 3.3 安装 Android Studio

```
下载地址: https://developer.android.com/studio

安装步骤:
1. 下载 Android Studio
2. 运行安装程序
3. 选择 "Standard" 安装类型
4. 等待 SDK、SDK Platform 和模拟器镜像下载完成
```

---

### 3.4 配置 Android SDK

打开 Android Studio → Settings → Appearance & Behavior → System Settings → Android SDK

**SDK Platforms**: 安装以下平台
- ✅ Android 13.0 (API 33)
- ✅ Android 14.0 (API 34)

**SDK Tools**: 安装以下工具
- ✅ Android SDK Build-Tools
- ✅ Android Emulator
- ✅ Android SDK Platform-Tools
- ✅ Intel x86 Emulator Accelerator (HAXM installer) [Windows/Mac]

---

### 3.5 配置环境变量

**macOS/Linux** (~/.zshrc 或 ~/.bash_profile):

```bash
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin
```

**Windows** (系统环境变量):

```
ANDROID_HOME = C:\Users\YourUsername\AppData\Local\Android\Sdk

Path 添加:
%ANDROID_HOME%\emulator
%ANDROID_HOME%\platform-tools
%ANDROID_HOME%\tools
%ANDROID_HOME%\tools\bin
```

---

### 3.6 创建 Android 虚拟设备

```bash
# 打开 Android Studio
# Tools → Device Manager → Create Device

# 推荐配置
设备: Pixel 6
系统镜像: Android 13 (API 33)
RAM: 推荐 4096MB 或更高
```

---

## 4. 微信小程序开发环境

### 4.1 微信开发者工具

```bash
# 下载地址
# Windows: https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
# macOS: https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

# 下载并安装微信开发者工具 (稳定版)
```

---

### 4.2 微信小程序账号注册

```
1. 访问微信公众平台
   https://mp.weixin.qq.com/

2. 注册小程序账号
   - 邮箱注册
   - 邮箱未注册过微信公众账号
   - 邮箱未注册过微信开放平台账号

3. 登录小程序后台
   - 获取 AppID
   - 配置服务器域名
   - 设置业务域名
```

---

### 4.3 安装 Taro CLI

```bash
# 使用 npm
npm install -g @tarojs/cli

# 或使用 pnpm (推荐)
pnpm add -g @tarojs/cli

# 验证安装
taro --version
```

---

### 4.4 Taro 项目依赖

```bash
# 在项目根目录安装依赖
pnpm install

# Taro 相关核心依赖
@tarojs/cli
@tarojs/components
@tarojs/react
@tarojs/runtime
@tarojs/taro
@tarojs/plugin-platform-weapp
```

---

## 5. H5开发环境

### 5.1 浏览器

**推荐浏览器**:
- Chrome (主要开发浏览器)
- Firefox (兼容性测试)
- Safari (iOS兼容性测试)
- Edge (兼容性测试)

---

### 5.2 安装依赖

```bash
# 在项目根目录
pnpm install

# H5 核心依赖
react
react-dom
react-router-dom
axios
```

---

### 5.3 开发服务器

```bash
# 启动开发服务器
npm run dev:h5

# 或
pnpm dev:h5

# 默认端口: 3000
# 访问: http://localhost:3000
```

---

## 6. 项目初始化

### 6.1 项目结构

```
zhouyi-app/                    # 项目根目录
├── apps/
│   ├── native/               # React Native 项目
│   │   ├── ios/              # iOS 原生代码
│   │   ├── android/          # Android 原生代码
│   │   └── src/              # React Native 源码
│   ├── miniprogram/          # Taro 小程序项目
│   │   ├── src/              # 小程序源码
│   │   └── project.config.json
│   └── h5/                   # React H5 项目
│       └── src/              # H5 源码
├── packages/                  # 共享包
│   ├── shared/               # 共享业务逻辑
│   ├── ui/                   # 共享 UI 组件
│   ├── utils/                # 工具函数
│   └── constants/            # 常量定义
├── docs/                      # 文档
├── .gitignore
├── package.json
├── pnpm-workspace.yaml
└── tsconfig.json
```

---

### 6.2 初始化 React Native 项目

```bash
# 进入 apps/native 目录
cd apps/native

# 使用 React Native CLI 创建项目
npx react-native@latest init ZhouYiApp --pm pnpm

# 或使用 Expo (推荐)
npx create-expo-app ZhouYiApp

cd ZhouYiApp

# 安装依赖
pnpm install

# 安装导航库
pnpm add @react-navigation/native @react-navigation/stack
pnpm add react-native-screens react-native-safe-area-context

# 安装状态管理
pnpm add zustand

# 安装网络请求库
pnpm add @tanstack/react-query axios

# iOS 安装 pods
cd ios && pod install && cd ..
```

---

### 6.3 初始化 Taro 小程序项目

```bash
# 进入 apps/miniprogram 目录
cd apps/miniprogram

# 使用 Taro CLI 初始化项目
taro init zhouyi-miniprogram

# 选择配置
? 请输入项目介绍: 周易通小程序
? 是否需要使用 TypeScript: Yes
? 请选择框架: React
? 是否需要使用 CSS 预处理器: Yes (选择 Sass)
? 请选择编译工具: Webpack5

# 安装依赖
cd zhouyi-miniprogram
pnpm install

# 安装状态管理
pnpm add zustand

# 安装网络请求库
pnpm add @tarojs/taro @tarojs/runtime
```

---

### 6.4 初始化 React H5 项目

```bash
# 进入 apps/h5 目录
cd apps/h5

# 使用 Vite 创建项目 (推荐)
pnpm create vite zhouyi-h5 --template react-ts

cd zhouyi-h5

# 安装依赖
pnpm install

# 安装路由
pnpm add react-router-dom

# 安装状态管理
pnpm add zustand

# 安装网络请求
pnpm add @tanstack/react-query axios

# 安装 UI 组件库 (可选)
pnpm add antd
```

---

### 6.5 创建 Monorepo 工作区

```bash
# 在项目根目录创建 pnpm-workspace.yaml
cat > pnpm-workspace.yaml << EOF
packages:
  - 'apps/*'
  - 'packages/*'
EOF

# 安装所有依赖
pnpm install
```

---

## 7. 代码规范配置

### 7.1 TypeScript 配置

创建 `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@shared/*": ["packages/shared/src/*"],
      "@ui/*": ["packages/ui/src/*"],
      "@utils/*": ["packages/utils/src/*"],
      "@constants/*": ["packages/constants/src/*"]
    }
  },
  "include": ["apps/*/src", "packages/*/src"],
  "exclude": ["node_modules", "dist", "build"]
}
```

---

### 7.2 ESLint 配置

安装 ESLint:

```bash
pnpm add -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin
pnpm add -D eslint-plugin-react eslint-plugin-react-hooks
```

创建 `.eslintrc.js`:

```javascript
module.exports = {
  root: true,
  parser: '@typescript-eslint/parser',
  plugins: ['@typescript-eslint', 'react', 'react-hooks'],
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react-hooks/recommended',
  ],
  parserOptions: {
    ecmaVersion: 2020,
    sourceType: 'module',
    ecmaFeatures: {
      jsx: true,
    },
  },
  settings: {
    react: {
      version: 'detect',
    },
  },
  rules: {
    'react/react-in-jsx-scope': 'off',
    'react/prop-types': 'off',
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
};
```

---

### 7.3 Prettier 配置

安装 Prettier:

```bash
pnpm add -D prettier eslint-config-prettier eslint-plugin-prettier
```

创建 `.prettierrc`:

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "arrowParens": "avoid"
}
```

---

### 7.4 Git Hooks (Husky)

```bash
# 安装 Husky 和 lint-staged
pnpm add -D husky lint-staged

# 初始化 Husky
pnpm exec husky install

# 添加 pre-commit hook
pnpm exec husky add .husky/pre-commit "pnpm exec lint-staged"
```

创建 `.lintstagedrc.json`:

```json
{
  "*.{ts,tsx,js,jsx}": ["eslint --fix", "prettier --write"],
  "*.{json,md}": ["prettier --write"]
}
```

---

## 8. 环境验证

### 8.1 验证脚本

创建 `scripts/verify-env.sh`:

```bash
#!/bin/bash

echo "🔍 验证开发环境..."

# Node.js
if command -v node &> /dev/null; then
  echo "✅ Node.js: $(node --version)"
else
  echo "❌ Node.js 未安装"
fi

# npm/pnpm/yarn
if command -v pnpm &> /dev/null; then
  echo "✅ pnpm: $(pnpm --version)"
elif command -v yarn &> /dev/null; then
  echo "✅ yarn: $(yarn --version)"
elif command -v npm &> /dev/null; then
  echo "✅ npm: $(npm --version)"
else
  echo "❌ 包管理器未安装"
fi

# Git
if command -v git &> /dev/null; then
  echo "✅ Git: $(git --version)"
else
  echo "❌ Git 未安装"
fi

# Java (Android)
if command -v java &> /dev/null; then
  echo "✅ Java: $(java -version 2>&1 | head -n 1)"
else
  echo "❌ Java 未安装 (Android开发需要)"
fi

# Xcode (iOS)
if command -v xcodebuild &> /dev/null; then
  echo "✅ Xcode: $(xcodebuild -version | head -n 1)"
else
  echo "❌ Xcode 未安装 (iOS开发需要)"
fi

# CocoaPods
if command -v pod &> /dev/null; then
  echo "✅ CocoaPods: $(pod --version)"
else
  echo "❌ CocoaPods 未安装 (iOS开发需要)"
fi

# Android SDK
if [ -n "$ANDROID_HOME" ]; then
  echo "✅ ANDROID_HOME: $ANDROID_HOME"
else
  echo "❌ ANDROID_HOME 未设置 (Android开发需要)"
fi

echo ""
echo "🎉 环境验证完成!"
```

运行验证:

```bash
chmod +x scripts/verify-env.sh
./scripts/verify-env.sh
```

---

### 8.2 运行各平台项目

```bash
# React Native - iOS
cd apps/native/ZhouYiApp
pnpm ios

# React Native - Android
pnpm android

# Taro 小程序
cd apps/miniprogram/zhouyi-miniprogram
pnpm dev:weapp
# 然后打开微信开发者工具，导入项目

# React H5
cd apps/h5/zhouyi-h5
pnpm dev
```

---

## 9. 常见问题

### 9.1 iOS 相关

**问题**: CocoaPods 安装失败

```bash
# 解决方案
sudo gem install cocoapods -n /usr/local/bin
```

**问题**: iOS 模拟器无法启动

```bash
# 重置模拟器
xcrun simctl erase all

# 重新启动模拟器
open -a Simulator
```

**问题**: pod install 很慢

```bash
# 使用中国镜像
pod repo remove master
pod repo add master https://mirrors.tuna.tsinghua.edu.cn/git/CocoaPods/Specs.git
```

---

### 9.2 Android 相关

**问题**: ANDROID_HOME 未设置

```bash
# macOS: 添加到 ~/.zshrc
export ANDROID_HOME=$HOME/Library/Android/sdk

# Windows: 设置系统环境变量
ANDROID_HOME = C:\Users\YourUsername\AppData\Local\Android\Sdk
```

**问题**: Gradle 构建失败

```bash
# 清理 Gradle 缓存
cd android
./gradlew clean

# 删除 .gradle 目录
rm -rf ~/.gradle
```

**问题**: 模拟器启动慢

```bash
# 在 BIOS 中启用 Intel VT-x 或 AMD-V
# 使用 Intel x86 镜像而非 ARM 镜像
```

---

### 9.3 通用问题

**问题**: npm install 很慢

```bash
# 使用中国镜像
npm config set registry https://registry.npmmirror.com

# 或使用 pnpm (推荐)
npm install -g pnpm
```

**问题**: 端口被占用

```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

---

### 9.4 调试技巧

**React Native 调试**:
- 摇晃设备/模拟器 → 点击 "Debug"
- 在 Chrome 中打开 http://localhost:8081/debugger-ui/
- 使用 React DevTools

**微信小程序调试**:
- 使用微信开发者工具的调试面板
- 在真机上预览扫码调试

**H5 调试**:
- Chrome DevTools (F12)
- React DevTools 扩展
- Redux DevTools 扩展

---

## 10. 下一步

环境搭建完成后，可以开始：

1. ✅ **子任务 1.3**: 后端技术栈选型和架构设计
2. ✅ **子任务 1.4**: 后端服务框架搭建
3. ✅ **子任务 1.5**: 数据库设计和初始化

---

**文档编写**: Claude
**最后更新**: 2026-01-11
**适用版本**: React Native 0.73+, Taro 3.6+, React 18+
